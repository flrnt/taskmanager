<h1>Mes tâches</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
     <%= task.name %>
     <%= task.description %>
     <%= task.status %>
     </li>
     <%= link_to "details", task_path(task) %>
  <% end %>
</ul>
